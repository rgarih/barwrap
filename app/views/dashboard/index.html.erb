<!--
<h1>Users:</h1>
<ul>
  <% @users.each do |user| %>
  <li data-user=<%= user.id %>>
  <%= user.email %>
  <% if current_user.is_following?(user.id) %>
    <%= link_to "Unfollow", unfollow_dashboard_path(user.id), method: :post %>
  <% else %>
    <%= link_to "Follow", follow_dashboard_path(user.id), method: :post %>
  <% end %>
</li>

<% end %>
</ul>
 -->
<h1>Users:</h1>
<ul>
  <%= render @users %>
</ul>
